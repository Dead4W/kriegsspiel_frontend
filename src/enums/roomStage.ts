export enum RoomStage {
  LOADING = 'loading',
  TEAM_SELECT = 'team_select',
  LOADING_MAP = 'loading_map',
  READY = 'ready',
  ERROR = 'error',
}

export enum RoomGameStage {
  PLANNING = 'planning',
  WAR = 'war',
  END = 'end'
}
